// background.js

// Этот файл может быть использован для обработки фоновых событий, если это необходимо.
// В данном случае он остается пустым, так как основная логика реализована в content.js.

// Пример: можно добавить обработчик для событий, если потребуется
chrome.runtime.onInstalled.addListener(() => {
  console.log("Расширение Image Selector установлено.");
});
